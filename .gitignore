/out
bin/
build/
/.vs
/.vscode
html
latex
.idea/
CMakeSettings.json
imgui.ini
EditorState.yaml
enc_temp_folder/
.cache
X3-Editor/res/EditorState.yaml
X3-Editor/res/imgui.ini

# Build artifacts
*.spv
compile_commands.json

# Slang toolchain, fetched by scripts/fetch-slang.sh. ~200 MB extracted and
# pinned by version in that script, so it is reproducible without being tracked.
X3/libs/slang/
